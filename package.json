{
    "test": "jest",
    "dependencies": {
        "nodemon": "^1.19.1",
        "webpack": "^4.35.3"
    },
    "scripts": {
        "start": "npm run build:live",
        "build": "webpack",
        "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
    },
    "devDependencies": {
        "webpack-cli": "^3.3.5"
    }
}
